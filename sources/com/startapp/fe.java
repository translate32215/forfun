package com.startapp;

import android.webkit.WebView;
import java.lang.ref.WeakReference;

/* compiled from: Sta */
public class fe extends WeakReference<WebView> {
    public fe(WebView webView) {
        super(webView);
    }
}
